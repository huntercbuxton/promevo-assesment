info:
  name: Get Label by Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: test-id

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
