info:
  name: Update a Gmail Label
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/update"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
